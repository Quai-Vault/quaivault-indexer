{
  "name": "quaivault-indexer",
  "version": "1.0.0",
  "description": "Indexing service for Quai Multisig wallets using Supabase",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "build": "tsc",
    "backfill": "tsx src/backfill.ts",
    "backfill:modules": "tsx src/module-backfill.ts",
    "reconcile:modules": "tsx src/module-reconcile.ts",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/ --max-warnings=0",
    "lint:fix": "eslint src/ --fix",
    "verify:gaps": "node scripts/verify-data-gaps.mjs",
    "verify:modules": "node scripts/verify-module-migration.mjs",
    "verify:modules:db": "psql \"$DATABASE_URL\" -f scripts/verify-module-postgres.sql",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "mkdir -p tests/e2e/logs && vitest run --config tests/e2e/vitest.e2e.config.ts 2>&1 | tee \"tests/e2e/logs/run-$(date +%Y%m%d-%H%M%S).log\"",
    "test:e2e:report": "mkdir -p tests/e2e/logs && vitest run --config tests/e2e/vitest.e2e.config.ts --reporter=json --outputFile=tests/e2e/logs/results.json"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "dotenv": "^16.3.1",
    "lru-cache": "^11.2.6",
    "pino": "^8.17.0",
    "pino-pretty": "^10.3.0",
    "pino-roll": "^4.0.0",
    "quais": "^1.0.0-alpha.53"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@types/node": "^20.10.0",
    "@vitest/coverage-v8": "^4.1.10",
    "eslint": "^9.0.0",
    "tsx": "^4.6.0",
    "typescript": "^5.3.0",
    "typescript-eslint": "^8.0.0",
    "vitest": "^4.1.10"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
